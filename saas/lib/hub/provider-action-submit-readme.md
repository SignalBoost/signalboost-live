# Provider action submit planner

`planProviderActionSubmission` maps a reviewed execution mode to the existing bounded route without executing it.

- Direct API routes to the existing provider executor.
- Governed AI infrastructure PR routes to proposal staging.
- Browser Agent assistance routes to dry-run packaging only.
- Direct configuration has no provider endpoint.
