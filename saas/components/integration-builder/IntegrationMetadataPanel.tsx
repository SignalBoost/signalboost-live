'use client'

import { LocalizedText } from '@/components/i18n/LocalizedText'
import SearchableDropdown from './SearchableDropdown'
import type { Option, Provider } from './mockApi'
export default function IntegrationMetadataPanel({ name, description, tags, providerId, tagOptions, providers, onName, onDescription, onTags, onProvider }: { name: string; description: string; tags: string[]; providerId: string; tagOptions: Option[]; providers: Provider[]; onName: (v: string) => void; onDescription: (v: string) => void; onTags: (v: string[]) => void; onProvider: (v: string) => void }) {
  return <section className="sb-glass-panel" style={{ padding: 18, display: 'grid', gap: 16 }}><h2 style={{ margin: 0, fontSize: 18 }}><LocalizedText fallback={"Integration Metadata"} /></h2><label style={{ display: 'grid', gap: 7, color: '#fff', fontWeight: 850, fontSize: 13 }}>Integration Name<input type="text" value={name} onChange={(e) => onName(e.target.value)} placeholder="Human-readable name" style={{ border: '1px solid rgba(255,255,255,.14)', borderRadius: 14, background: 'rgba(2,6,23,.78)', color: '#fff', padding: 12 }} /></label><label style={{ display: 'grid', gap: 7, color: '#fff', fontWeight: 850, fontSize: 13 }}>Description<textarea value={description} onChange={(e) => onDescription(e.target.value)} placeholder="Human-readable purpose only" rows={4} style={{ border: '1px solid rgba(255,255,255,.14)', borderRadius: 14, background: 'rgba(2,6,23,.78)', color: '#fff', padding: 12, resize: 'vertical' }} /></label><SearchableDropdown label="Tags" options={tagOptions} value={tags} onChange={(v) => onTags(v as string[])} multiple placeholder="Search tags" /><SearchableDropdown label="Provider" options={providers.map((p) => ({ id: p.id, label: p.name, description: p.description, icon: p.icon }))} value={providerId} onChange={(v) => onProvider(v as string)} placeholder="Search providers" /></section>
}
