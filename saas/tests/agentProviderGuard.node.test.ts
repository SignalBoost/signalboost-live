import test from 'node:test';import assert from 'node:assert/strict';import {guardAgentSandboxProvider}from '../lib/agent-runtime/provider-guard.ts';import {loadAgentSandboxProviderConfig}from '../lib/agent-runtime/provider-config.ts';import {DEFAULT_SANDBOX_RUNTIME_POLICY}from '../lib/agent-runtime/policy.ts';const base=()=>({principal:{privileged:true,executionEntitled:true},toolPolicy:{permitsCodeExecution:true},providerConfig:loadAgentSandboxProviderConfig({AGENT_SANDBOX_PROVIDER:'remote',AGENT_SANDBOX_ENABLED:'true',AGENT_SANDBOX_ENDPOINT:'https://sandbox.example.com',AGENT_SANDBOX_TOKEN:'secret'}),runtimePolicy:DEFAULT_SANDBOX_RUNTIME_POLICY,quota:{concurrentSessions:0,sessionsInLastHour:0,workflowCostUnits:0,available:true},language:'javascript' as const,capabilities:[] as const,supportedLanguages:['javascript'] as const,supportedCapabilities:[] as const});test('guard requires conditions',()=>{assert.equal(guardAgentSandboxProvider(base()).authorized,true);assert.equal(guardAgentSandboxProvider({...base(),principal:{privileged:false,executionEntitled:true}}).authorized,false);assert.equal(guardAgentSandboxProvider({...base(),runtimePolicy:{...DEFAULT_SANDBOX_RUNTIME_POLICY,outboundNetwork:true}}).authorized,false)})
