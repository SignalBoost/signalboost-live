// saas/lib/config/unifiedPricing.ts
// DEPRECATED PATH — kept as a thin forwarder so existing imports keep working.
// The single source of truth is ./unifiedPricingConfig. Prefer importing from
// there directly in new code; this file may be deleted once all imports move.
export * from './unifiedPricingConfig'
