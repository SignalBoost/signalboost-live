import test from 'node:test'; import assert from 'node:assert/strict'; import { loadAgentSandboxProviderConfig } from '../lib/agent-runtime/provider-config.ts'; test('configuration is disabled by default and blocks unsafe remote endpoints',()=>{assert.equal(loadAgentSandboxProviderConfig({}).providerId,'disabled');for(const endpoint of ['http://example.com','https://user:pass@example.com','https://example.com/?token=x','https://localhost','https://127.0.0.1','https://[::1]','https://169.254.1.1','https://192.168.1.1'])assert.throws(()=>loadAgentSandboxProviderConfig({AGENT_SANDBOX_PROVIDER:'remote',AGENT_SANDBOX_ENABLED:'true',AGENT_SANDBOX_ENDPOINT:endpoint,AGENT_SANDBOX_TOKEN:'secret'}));assert.throws(()=>loadAgentSandboxProviderConfig({AGENT_SANDBOX_PROVIDER:'unknown'}));const c=loadAgentSandboxProviderConfig({AGENT_SANDBOX_PROVIDER:'remote',AGENT_SANDBOX_ENABLED:'true',AGENT_SANDBOX_ENDPOINT:'https://sandbox.example.com',AGENT_SANDBOX_TOKEN:'secret',UNRELATED:'leak'});assert.equal(JSON.stringify(c).includes('leak'),false);assert.throws(()=>loadAgentSandboxProviderConfig({AGENT_SANDBOX_PROVIDER:'remote',AGENT_SANDBOX_ENABLED:'true',AGENT_SANDBOX_ENDPOINT:'https://sandbox.example.com',AGENT_SANDBOX_TOKEN:'s',AGENT_SANDBOX_REQUEST_TIMEOUT_MS:'0'}));})
