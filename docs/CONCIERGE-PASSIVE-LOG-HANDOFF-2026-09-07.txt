Concierge passive operational-log handoff — 2026-09-07

Observed behavior:
- A passive pasted Vercel failure was diagnosed correctly.
- Concierge did not mutate code without explicit repair intent.
- The original reply asked the user to attach source code even though an authorized SignalBoost repair can inspect its repository once the user explicitly requests repair.
- PR #1928 removed that source-demand wording and added the natural two-turn log -> "fix it" evidence handoff.
- The first live post-#1928 passive-log test still omitted the "fix it" offer after a successful neural diagnosis. The deterministic fallback contained the handoff, but `diagnoseOperationalLog()` returned a successful model answer directly, bypassing the fallback/action suffix.

Required behavior:
- Passive logs remain read-only evidence.
- Concierge explains the failed command/checks concisely.
- Failure words inside the log never manufacture repair authority.
- Concierge does not demand repository source up front when source may already be available to the authorized Builder workspace/repository.
- It offers an explicit repair handoff rather than delegating repository discovery back to the user.
- A richer neural diagnosis may improve specificity but may not remove the host-owned repair handoff.
- The exact command phrase "fix it" remains visible in every supported Concierge language so the next turn has an unambiguous repair intent token.
- If source is genuinely unavailable, Builder may ask for it then.
- If the current surface lacks repair authority, it says so rather than pretending to act.

Acceptance:
- Focused pastedOperationalLog regression must pass.
- A successful neural diagnostic reply must be post-processed by the host to retain the deterministic "fix it" / Builder handoff.
- The two-turn log -> explicit repair handoff must preserve the prior failure evidence.
- Full mandatory gates, TypeScript, Preview, normal two-parent PR merge, and Production READY remain required.
- A new live passive-log turn after deployment must visibly offer "fix it" before this behavior is considered runtime-accepted.

Repository-write note:
- A placeholder version of this file was accidentally created directly on main before the first repair branch existed. That was an integration-discipline mistake, not a functional source change. The governed PR replaced the placeholder with the real record; this follow-up keeps the live acceptance history explicit rather than rewriting it.
