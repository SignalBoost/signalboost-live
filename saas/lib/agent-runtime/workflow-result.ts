import type { AgentWorkflowAuditEvent, AgentWorkflowCleanupStatus, AgentWorkflowDenial, AgentWorkflowExecutionFailure, AgentWorkflowTimingMetadata, AgentWorkflowVerifiedSuccess } from './workflow-types.ts'
const safe = (value: string) => value.replace(/(?:bearer\s+\S+|(?:api[_ -]?key|token|secret|password|authorization)\s*[=:]\s*\S+|process\.env|stdout|stderr|source|tests?|artifact|https?:\/\/\S+)/gi, '[REDACTED]').slice(0, 256)
const common = (timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, auditEvents: readonly AgentWorkflowAuditEvent[]) => ({ timing: Object.freeze({ ...timing }), cleanup: Object.freeze({ ...cleanup }), auditEvents: Object.freeze([...auditEvents]) })
export const authorizationDenial = (reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowDenial => Object.freeze({ kind: 'denial', reason: safe(reason), stage: 'authorization', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) })
export const quotaDenial = (reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowDenial => Object.freeze({ kind: 'denial', reason: safe(reason), stage: 'quota_reservation', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) })
export const providerUnavailable = (reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure => Object.freeze({ kind: 'failure', category: 'sandbox_unavailable', failedStage: 'provider_resolution', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) })
export const repairFailure = (category: string, diagnostics: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure => Object.freeze({ kind: 'failure', category: safe(category), failedStage: 'repair', diagnostics: Object.freeze([safe(diagnostics)]), ...common(timing, cleanup, events) })
export const verifiedSuccess = (attempts: number, corrections: number, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowVerifiedSuccess => Object.freeze({ kind: 'success', verified: true, attempts: Math.max(0, attempts), corrections: Math.max(0, corrections), ...common(timing, cleanup, events) })
export const internalBoundedFailure = (stage: AgentWorkflowExecutionFailure['failedStage'], diagnostics: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure => Object.freeze({ kind: 'failure', category: 'internal', failedStage: stage, diagnostics: Object.freeze([safe(diagnostics)]), ...common(timing, cleanup, events) })
