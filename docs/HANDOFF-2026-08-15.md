# SignalBoost Engineering Handoff — 2026-08-15

This is the late-2026-08-15 takeover checkpoint. Read it with `ONBOARD.md`, then re-scan current `main`, open PRs, Vercel and Supabase before changing or reporting state. Multiple agents are working concurrently; newer repository/runtime evidence always wins.

## Last verified code-bearing production checkpoint

The last verified **code-bearing** main commit before these docs-only handoff updates was:

- `e7be3525c453170ac79858fc78a10eb235e2937a` — Merge PR #1221, `repair RunPod boot contract before wake`;
- exact Vercel production deployment: `dpl_CfRYftcFAPLMEHuLzEhmqUajEeVr`;
- deployment state: `READY`;
- deployment commit matched `e7be3525...` exactly.

This handoff file was refreshed afterward with documentation-only commit(s), so **do not use the repository HEAD encoded in this document as a production-code claim**. Re-query current `main` and Vercel before saying production is green.

## Whole-platform status

Use approximately **92% overall** for the complete commercial platform unless a fresh subsystem-by-subsystem scan justifies a change. The older ~98% figure in `ONBOARD.md` reflects architecture/core maturity more than complete commercial readiness.

Remaining work is concentrated in production integrations, commercial packaging/fulfillment/acceptance, the real 5,000-company corpus, closed-loop revenue learning, objective-outcome evidence accumulation, stale-PR reconciliation, and production hardening rather than foundational COS architecture.

Do not increase completion merely because tests, docs, CI wiring, or another green deployment were added.

## COS north star

Build a continuously learning digital agent that accumulates verified skills from experience, practices them, combines them, learns from better teachers when necessary, and becomes progressively more autonomous without pretending to be perfect.

```text
PERCEIVE → RECALL → THINK → INVESTIGATE/TOOLS → TEST → ANSWER/ACT
→ FEEDBACK → REFLECT → LEARN → PRACTICE UNSEEN VARIANTS
→ CONSOLIDATE → RETAIN/RE-TEST → WEAKEN/FORGET/QUARANTINE
→ COMPOSE SKILLS → TEST TRANSFER → METACOGNITION → COUNCIL WHEN WARRANTED
```

The durable asset is COS memory, knowledge, skills, governance, tools, provenance, verification and learning. Models/providers are replaceable compute/teachers.

## Confidence doctrine

Confidence is a calibrated estimate of correctness, not a target to inflate.

- never raise confidence merely to cross a threshold;
- evaluate calibration empirically (ECE/Brier/log loss/bias as appropriate);
- provenance inconsistent with telemetry is a credibility failure;
- skill status, Council agreement, lifecycle maturity, composition or metacognition do not automatically add answer-confidence credit;
- Council confidence is advisory only.

## Council / objective-learning chain on main

Preserve these invariants:

1. Council members form independent first opinions.
2. Relevant roles may pre-register bounded machine-checkable predictions.
3. Challenge/rebuttal exchanges claims, evidence, assumptions and falsifiers — not hidden chain-of-thought.
4. Objective outcomes come only from deterministic tools, production outcomes or authoritative records.
5. Exact correlation binds evidence to the relevant Council session/outcome.
6. Predictions resolve mechanically from bounded facts; no model interpretation is used for claim resolution.
7. Role credibility changes only when registered claims are actually resolved.
8. Successful operations do not become blanket specialist correctness.
9. Refuted specialist predictions open/reopen learning gaps rather than automatically damaging unrelated skills.
10. Council participation/agreement never creates an automatic answer-confidence bonus.

Important merged checkpoints: #1195, #1198, #1199, #1208, #1209 and #1215.

## Exact Self-Healing / Vercel outcome verification — PR #1215

Merged as `53e327bbb8931bae30745da07597a7839937569a`.

- repair verification is bound to the **exact Vercel deployment ID** created by the governed operation;
- no unrelated newer READY deployment can certify the repair;
- READY = success; ERROR/CANCELED/CANCELLED = failure; non-terminal states remain unresolved and are checked later;
- exact Self-Healing verification requires authenticated Vercel deployment identity and fails closed if exact identity is unavailable;
- follow-up objective evidence inherits the exact parent Council outcome/session instead of fuzzy-rematching recurring incident fingerprints;
- evidence remains bounded and immutable;
- the non-destructive native Self-Healing acceptance trace is gated in CI.

Key files:

- `saas/self-healing-host/vercel-deployment-outcome-verifier.ts`
- `saas/self-healing-host/vercel-deployment-verification-pure.ts`
- `saas/lib/ai/cos/councilObjectiveOutcome.ts`
- `saas/lib/infra-pr/redeploy.ts`
- `saas/tests/vercelExactRepairVerification.node.test.ts`
- `saas/tests/selfHealingNativeAcceptance.node.test.ts`

Supabase migration `20260815_cos_council_followup_objective_outcomes.sql` was applied to `qpblefwtnbivuusxmabv` and verified service-role-only: RPC exists; `anon` and `authenticated` cannot execute; `service_role` can execute.

No synthetic production outcomes were inserted merely to inflate evidence. At the verification point deterministic/production/authoritative objective counts were zero; re-query before reporting current counts.

## RunPod lifecycle / cost controls now on main

Preserve this sequence; do not reintroduce older behavior.

### PR #1217 — background no-wake

`f7bcd846d55bcf9148639af87e9c703b74bea806`

- background Supervisor diagnostics may use local Qwen only if already healthy;
- stopped/unhealthy RunPod must not be resumed for background diagnostics;
- governed fallback is used instead;
- interactive COS wake-on-demand remains allowed.

### PR #1218 — failed cold-wake fail-safe

`dec42deee630bfb1d95b73def4560f5ad226d5de`

- lifecycle decisions use authoritative RunPod state;
- if a COS request resumes the pod and cold-start readiness fails, that request owns cleanup and stops it;
- idle-stop emits non-secret authoritative state telemetry.

### PR #1219 — stale unhealthy orphan stop

Merged as the code parent `bbd2c2366c9f83b2141f40d0a404e767d4b7aa19` before #1221.

- RUNNING RunPod compute that remains unhealthy beyond the cold-start grace window is stopped even if failed-request activity timestamps would otherwise keep it alive;
- healthy-pod idle-stop semantics remain intact.

### PR #1221 — repair boot contract before wake

`e7be3525c453170ac79858fc78a10eb235e2937a`

- intentional COS wake-on-demand verifies/repairs the RunPod Docker startup contract before compute resumes;
- the persistent `/workspace` reasoner bootstrap becomes the cold-start startup contract;
- wake tracking records whether the request started compute/repaired the contract so the cold-start fail-safe owns cleanup correctly;
- background no-wake and orphan/failure cost protections are preserved.

Do **not** claim Qwen3 is live from these lifecycle changes. The last verified development reasoner remains `qwen2.5-coder:32b` unless newer runtime telemetry proves otherwise.

## Open PR #1220 — replacement for stale #1121

At the final check before this handoff, PR #1220 `Self-Healing: preserve portable capability execution policy` was **open**, not merged.

- head: `9d851402da40c628d002143987a5315ff38e7549`;
- branch: `fix/self-healing-portable-capability-policy-20260815`;
- preview head observed READY: `dpl_5SDigBM9AqsTYCjFXdLwWn8VXfz4`;
- it was based on `dec42dee...`, while code-bearing main later advanced through #1219/#1221;
- GitHub reported it non-mergeable at the handoff check.

Next agent must re-query #1220, compare/rebase/refresh it against current main, and re-verify the exact preview before merging. Do not merge stale #1121 directly.

Desired semantics:

- registered read-only observation remains unattended;
- registered routine/reversible mutation may remain unattended only when `autoExecutable=true` and `approvalRequired=false`;
- consequential or internally inconsistent declarations fail closed / remain approval-gated;
- buyers may choose stricter approval;
- caller metadata cannot overwrite authoritative governance facts;
- unknown/unregistered capabilities fail closed.

After #1220 safely merges, close stale #1121 as superseded with an explanatory comment.

## Stale PR reconciliation already completed

Closed as superseded/ported:

- #1181 — config-drift recovery already on main through #1190;
- #1122 — native-monitoring contract superseded by stronger main implementation; useful regression retained;
- #1175 — useful non-destructive acceptance test/doc ported into #1215;
- #1173 — weak “newer deployment” Vercel verifier superseded by #1215 exact-deployment verification.

Do not close old PRs solely because of age. Compare exact value against current main, then port the useful delta or close as demonstrably superseded.

Important remaining stale PRs include #1099, #1093, #1059, #1054, #1041, #1037, #965, #964 and older outreach/i18n/commercial-readiness work.

## GitHub Actions warning

Several Actions runs during this work showed failures with **zero job steps and no logs**. Treat those as runner/infrastructure failures, not evidence that application tests ran and failed.

Do not report code test pass/fail from a zero-step run. Inspect job steps/logs first. Fixing Actions infrastructure remains separate work.

## Active-learning / skill state to preserve

Validated skill: `diagnose-tenant-specific-tail-latency`.

Earlier verified evidence: evaluator approved; understanding approved; practice 2/2; unseen holdout 3/3 across distinct variants; failures 0; production outcomes 0 at that snapshot.

Retention is evidence-driven. Validation cadence, contradiction handling, weakening and quarantine remain separate from answer confidence.

Known edge worth inspecting before claiming fixed: evaluator-approved + understanding-false may remain evaluated without automatic retry/reteach depending on the current state machine.

## Self-Healing architecture

Native monitoring is default; external monitoring is optional/hybrid. Signal families include service/API latency/error, queues/jobs/providers, DB, storage/persistence, TLS, resource pressure, config drift and deployment health.

Routine registered reversible repairs may auto-execute under explicit policy. Consequential actions remain approval-gated.

Config-drift recovery is deterministic: desired cadence from deployed scheduler; system-owned policy only; optimistic concurrency; independent DB readback; guarded rollback; `verified=true` only after objective state proves success.

## Portable / BYOM doctrine

The product is the Portable + COS brain, not Qwen.

```text
Model / Agent → COS → governance → Portable Connector Runtime
→ approval when required → execution → verification / audit
```

Buyers may use OpenAI, Anthropic, Azure, Gemini, private/open models or other compatible compute. COS-owned memory, knowledge, skills, governance, provenance, verification and learning remain the corporate intelligence.

**The model changes. The corporate intelligence stays.**

## Business corpus

Target remains approximately 5,000 **real** companies for internal-first business intelligence. Do not fabricate rows to hit the number.

COS should check internal intelligence before paid/external enrichment. Useful verified enrichment should persist back into the owned corpus/memory/graph under current governance. Re-measure actual real-company coverage before reporting completion.

## Commercial readiness

Canonical fail-closed ten dimensions:

1. architecture;
2. distribution package;
3. integrity manifest;
4. buyer installation;
5. licensing enforcement;
6. fulfillment handoff;
7. operations/recovery;
8. buyer configuration;
9. deployment acceptance;
10. support boundary.

Missing evidence blocks readiness. Do not infer commercial readiness from architecture, docs, checkout code or a green deployment alone.

## Recommended next work

1. Re-scan newest `main`, Vercel production and open PRs.
2. Finish/rebase/reverify #1220 on current main; merge only if sound, then close stale #1121.
3. Broaden objective outcome coverage beyond exact Vercel repairs to other deterministic Self-Healing verifications (DB readback/migrations/config drift/etc.) using exact parent binding and bounded facts only.
4. Fix the GitHub Actions zero-step runner/infrastructure problem separately from application code.
5. Continue stale-PR reconciliation via exact current-main diff; port useful deltas rather than blind merges.
6. Finish production CRM integrations: Salesforce, HubSpot, Zoho, Pipedrive, plus remaining communication/data-provider adapters.
7. Complete the real 5,000-company corpus with no fabricated companies.
8. Close the outreach → reply → meeting → opportunity → sale → revenue learning loop.
9. Complete packaging, integrity, licensing, fulfillment, recovery, clean-environment buyer install, deployment acceptance and support evidence for every Portable.
10. Accumulate real objective outcomes/credibility evidence; never synthesize outcomes to inflate proof.

## Operational rule for the next agent

Before any code/status claim:

1. fetch current `main`;
2. inspect recent commits and overlapping open PRs;
3. inspect exact Vercel production commit/deployment;
4. inspect relevant Supabase migration/runtime state when DB behavior matters;
5. avoid duplicating another active agent's branch;
6. verify the exact changed runtime before merging;
7. refresh the handoff after a meaningful checkpoint.

Never report production behavior from memory alone.
