import type { PortableBrowserAdapterDescriptor } from './browser-adapter-descriptor.ts'
export class PortableBrowserAdapterRegistry { private readonly entries=new Map<string,PortableBrowserAdapterDescriptor>(); register(d:PortableBrowserAdapterDescriptor){if(this.entries.has(d.adapterId))throw new Error('duplicate_browser_adapter');this.entries.set(d.adapterId,d);return d} get(id:string){const d=this.entries.get(id);if(!d)throw new Error('unknown_browser_adapter');return d} list(){return Object.freeze([...this.entries.values()].sort((a,b)=>a.adapterId.localeCompare(b.adapterId)))} }
