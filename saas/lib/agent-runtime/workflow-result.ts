import type { AgentWorkflowAuditEvent, AgentWorkflowCleanupStatus, AgentWorkflowDenial, AgentWorkflowExecutionFailure, AgentWorkflowResult, AgentWorkflowTimingMetadata, AgentWorkflowVerifiedSuccess } from './workflow-types.ts'

const safe = (value: string) => value.replace(/(?:bearer\s+\S+|(?:api[_ -]?key|token|secret|password)\s*[=:]\s*\S+|process\.env|stdout|stderr|source|tests?|artifact)/gi, '[REDACTED]').slice(0, 256)
const freezeTiming = (timing: AgentWorkflowTimingMetadata) => Object.freeze({ ...timing })
const freezeCleanup = (cleanup: AgentWorkflowCleanupStatus) => Object.freeze({ ...cleanup })
const common = (timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, auditEvents: readonly AgentWorkflowAuditEvent[]) => ({ timing: freezeTiming(timing), cleanup: freezeCleanup(cleanup), auditEvents: Object.freeze([...auditEvents]) })
export function authorizationDenial(reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowDenial { return Object.freeze({ kind: 'denial', reason: safe(reason), stage: 'authorization', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) }) }
export function quotaDenial(reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowDenial { return Object.freeze({ kind: 'denial', reason: safe(reason), stage: 'quota_reservation', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) }) }
export function providerUnavailable(reason: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure { return Object.freeze({ kind: 'failure', category: 'sandbox_unavailable', failedStage: 'provider_resolution', diagnostics: Object.freeze([safe(reason)]), ...common(timing, cleanup, events) }) }
export function repairFailure(category: string, diagnostics: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure { return Object.freeze({ kind: 'failure', category: safe(category), failedStage: 'repair', diagnostics: Object.freeze([safe(diagnostics)]), ...common(timing, cleanup, events) }) }
export function verifiedSuccess(attempts: number, corrections: number, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowVerifiedSuccess { return Object.freeze({ kind: 'success', verified: true, attempts: Math.max(0, attempts), corrections: Math.max(0, corrections), ...common(timing, cleanup, events) }) }
export function internalBoundedFailure(stage: AgentWorkflowExecutionFailure['failedStage'], diagnostics: string, timing: AgentWorkflowTimingMetadata, cleanup: AgentWorkflowCleanupStatus, events: readonly AgentWorkflowAuditEvent[]): AgentWorkflowExecutionFailure { return Object.freeze({ kind: 'failure', category: 'internal', failedStage: stage, diagnostics: Object.freeze([safe(diagnostics)]), ...common(timing, cleanup, events) }) }
export type { AgentWorkflowResult }
