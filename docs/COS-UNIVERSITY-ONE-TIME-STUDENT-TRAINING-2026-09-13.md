# COS University — One-Time Student Training

**Date:** September 13, 2026

This increment permits exactly one owner-approved `distillation` training dispatch for the qualified Software Specialist `reasoning_decision_science` candidate after successful teacher generation and immutable train/holdout partition registration.

The capability is bound to the exact candidate, `Qwen/Qwen3-4B` base model, dataset hash, fine-tune revision key, and **NVIDIA T4 Small (`t4-small`)**. It hard-caps the live provider rate at **$0.41/hour** and total estimated compute exposure at **$1.61**. It expires within 15 minutes and is atomically claimed before provider submission.

The capability records the existing revision-bound `dataset_approved` and `training_approved` host claims immediately before dispatch, but it cannot expand authority or authorize model promotion. Training completion must still produce signed training-executor artifact and rollback evidence. Independent evaluation, safety regression, unseen transfer, delayed retention, Production canary, rollback proof, and promotion remain separate gates.
