import test from 'node:test'; import assert from 'node:assert/strict'; import { SupabaseAgentWorkflowRecoveryLock } from '../lib/agent-runtime/workflow-recovery-lock.ts'
test('recovery lock hashes token and delegates atomic operations',async()=>{let tokenHash='';const db:any={acquireRecoveryLock:async(i:any)=>{tokenHash=i.tokenHash;return true},renewRecoveryLock:async()=>false,releaseRecoveryLock:async()=>false};const lock=new SupabaseAgentWorkflowRecoveryLock(db);assert.equal(await lock.acquire({ownerId:'owner-1',token:'plain-token',now:0,durationMs:1000}),true);assert.notEqual(tokenHash,'plain-token');await assert.rejects(()=>lock.acquire({ownerId:'owner-1',token:'x',now:0,durationMs:1}))})
