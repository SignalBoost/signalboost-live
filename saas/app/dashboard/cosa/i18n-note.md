# COSA dashboard i18n requirement

The COSA dashboard must not ship user-facing hard-coded English strings.

Use the shared dashboard i18n provider or the portable COS dictionary helpers for every label, button, empty state, status message, and error message.

Required languages: en, es, pt, pl, ru.
