import type { ProviderCapability } from './provider-capability.ts'; import type { OriginProfile } from './provider-origin.ts'; import type { NavigationProfile } from './provider-navigation.ts'; import type { ProviderSelector } from './provider-selector.ts'; import type { VerificationProfile } from './provider-verification.ts'; import type { EvidenceProfile } from './provider-evidence.ts'; import type { ProviderHealth } from './provider-health.ts'; import type { ProviderVersion } from './provider-version.ts'
export type Locale='en'|'es'|'pt'|'pl'|'ru'; export type LocalizedText=Readonly<Record<Locale,string>>
export type ExecutionMode='read_only'
export interface BrowserProviderAdapter { id:string; displayName:LocalizedText; capabilities:readonly ProviderCapability[]; origins:readonly OriginProfile[]; selectors:readonly ProviderSelector[]; navigation:readonly NavigationProfile[]; verification:readonly VerificationProfile[]; evidence:readonly EvidenceProfile[]; health:ProviderHealth; version:ProviderVersion; executionModes:readonly ExecutionMode[]; autoFailoverSupported:boolean; browserOnDemandSupported:boolean; readOnlySupported:true; productionSupported:boolean }
export function freezeProvider(a:BrowserProviderAdapter):BrowserProviderAdapter{return Object.freeze({...a,displayName:Object.freeze({...a.displayName}),capabilities:Object.freeze([...a.capabilities]),origins:Object.freeze([...a.origins]),selectors:Object.freeze([...a.selectors]),navigation:Object.freeze([...a.navigation]),verification:Object.freeze([...a.verification]),evidence:Object.freeze([...a.evidence]),executionModes:Object.freeze([...a.executionModes]),health:Object.freeze({...a.health}),version:Object.freeze({...a.version})})}
