# University agent-bound capstone execution

Date: 2026-09-11. Companion: ONBOARD.md, SKILLS.md, COS-UNIVERSITY-GRADUATION-REMEDIATION-GATE.md.

## Scope

Replace the unsupported Software Specialist capstone deferral with a real host-selected software execution branch. COS keeps its existing generalist answer path. A registered non-COS software_engineering agent uses the exact configured Builder/Software Specialist model through the existing approved local-inference boundary. Unknown agents, other unimplemented roles, a missing model, failed storage, empty/truncated inference and a role change during inference fail closed. There is no COS answer, shared-cache, hosted-teacher or provider fallback.

The specialist receives the case prompt and only its own validated/learned/mastered University procedures. Both metadata and provenance must match its identity; evaluator/understanding approval and a valid past validation time are required. Unvalidated practice, composites with dependencies, foreign agents and private rubrics are excluded. An empty valid procedure set is disclosed as empty; unavailable storage is an error, not an empty success. This is bounded procedural context, not a claim of complete learner-memory retrieval.

Host execution provenance records the learner, persisted role, run, fresh trace ID, explicitly selected model, manifest, input/context/output hashes, timestamps and available deployment/commit identity. It contains neither provider credentials, hidden reasoning nor the hidden rubric. The selected model is the model requested by the approved host transport; it is not an independently attested provider model implementation.

The existing independent host scorer determines pass/fail. The executor itself has academicAuthority=none. A stored receipt must match the exact learner, run, trace and manifest before a specialist capstone can count; a relabeled COS row or copied specialist receipt cannot earn credit in either direction. COS outcome records are not reused for specialist turns.

## Boundaries preserved

Each agent still needs its own academic prerequisites, independent scoring, current evidence, minimum residence, two distinct capstone passes, no unresolved remediation and duplicate-safe credential issuance. Master’s admission remains deferred for specialists until those workers are agent-scoped. Other specialist execution roles are not claimed. No minimum-residence date, grading profile, scorer, assessment result or runtime authority changes.

## Validation and rollout

32 focused Node tests pass: 16 new capstone tests, 14 existing runtime-policy tests and two credential-key tests. Existing runner, gate, policy and policy-test baselines were matched against exact Git blob hashes before changes. The local environment is a focused source assembly because repository cloning could not resolve GitHub; full repository CI/typechecking and Preview remain required on the exact PR head.

The additive provenance migration was applied as 20260911201757. The constraint applies to new writes; NOT VALID preserves historical records without rewriting or inventing execution provenance. At verification, both capstone and credential tables contained zero records. No synthetic learner result or Production receipt was inserted.

Implementation and schema installation do not prove an eligible specialist capstone has executed in Production. Current agents remain blocked by academic/residence/remediation requirements. Do not bypass those gates, fabricate prerequisites, move residence dates or record a test fixture as a Production result. Exact-merged-commit route evidence and eventual genuine eligible capstone execution remain separate acceptance steps.
