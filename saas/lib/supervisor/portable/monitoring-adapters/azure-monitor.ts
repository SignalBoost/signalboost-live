import type { IncidentSourceDefinition } from '../incident-source.ts'
import { first, isNonIncidentState, mapping, object } from './shared.ts'
export function createAzureMonitorDefinition(sourceId:string):IncidentSourceDefinition{return{sourceId,vendor:'Azure Monitor',status:'staged',map(body,delivery){const root=object(body),data=object(root.data),essentials=object(first(data.essentials,root.essentials));if(isNonIncidentState(essentials.monitorCondition,essentials.alertState,root.status))return null;return mapping({provider:'azure',delivery,message:first(essentials.description,root.message,essentials.alertRule),severity:first(essentials.severity,root.severity),environment:first(essentials.environment,root.environment),detectedAt:first(essentials.firedDateTime,root.eventTimestamp),errorCode:first(essentials.alertId,root.id),affectedResource:first(Array.isArray(essentials.alertTargetIDs)?essentials.alertTargetIDs[0]:essentials.alertTargetIDs,root.resourceId),dedupeKey:first(essentials.alertId,essentials.alertRule),metadata:{adapterId:'azure-monitor',adapterMaturity:'staged'}})}}}
