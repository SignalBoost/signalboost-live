// saas/lib/_supervisor-drill.ts
// Deliberate syntax error. This file exists to fail one PREVIEW build so the
// Self-Healing Supervisor receives its first real incident. Delete after the drill.
export function drill() {
  return 'unclosed
}
