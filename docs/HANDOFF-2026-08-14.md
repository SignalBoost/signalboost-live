# SignalBoost COS Engineering Handoff — 2026-08-14

This file is the current takeover state for the COS work completed late 2026-08-13 America/Managua / early 2026-08-14 UTC. For the topics below, this dated handoff and live repository/runtime/database evidence supersede older static counts in `ONBOARD.md` and earlier handoffs.

## Read first

1. `ONBOARD.md`
2. **This file**
3. `docs/HANDOFF-COS-ACTIVE-LEARNING-2026-08-13.md`
4. `docs/COS-COGNITIVE-COUNCIL-HANDOFF-2026-08-13.md` if present / otherwise inspect PR #1195 and `saas/lib/ai/cos/cognitiveCouncil.ts`
5. Current `main`
6. Live Vercel + Supabase state before making any status claim

Do not report production green from memory. Verify the exact deployment/commit.

---

# Current production

Repository: `SignalBoost/signalboost-live`

Current `main` at this handoff:

- **`137c7ba521f37b0a5019f915366dc24533aa0f1e`**
- merge message: `Merge PR #1196: preserve actual external provider provenance`

Exact production deployment verified READY:

- **`dpl_DxR34uk6GRJwPK4FQ7RkUciRimVy`**
- target: `production`
- commit: `137c7ba521f37b0a5019f915366dc24533aa0f1e`
- aliases include `saas.signalboostapp.com`
- `aliasError: null`

Immediately preceding major merges:

- **PR #1193** — COS embedding backfill auth/status UX
- **PR #1195** — first bounded COS Cognitive Council layer
- **PR #1196** — truthful actual external-provider/model provenance

PR #1196 was rebased on the Council production head and preserves Council behavior.

---

# Foundational learned-corpus embeddings

The owner successfully used the Hub control and initially completed the governed embedding backfill:

- total retained at that moment: 75
- eligible: 61
- eligible embedded: 61
- pending: 0
- quarantined / relevance-rejected: 14
- failures: 0

The 61 embedded vectors were verified in production as **768 dimensions**.

Important: **total retained knowledge does not decrease when embeddings are created**. Quarantined `relevance_rejected` rows intentionally remain unembedded.

## Latest live DB state at this handoff

The corpus continued learning after the successful 61-row backfill. Latest production query now shows:

- **total: 92**
- **eligible: 78**
- **eligible embedded: 61**
- **eligible pending: 17**
- **quarantined: 14**

Current source-kind counts:

- `benchmark_fixture`: 1
- `official_documentation`: 2
- `scientific_journal`: 78
- `video_transcript`: 11

The latest new scientific-journal observation was around `2026-08-14 05:59:24Z`.

Therefore the next agent must NOT assume the Hub still shows pending=0. Refresh status first.

### Next embedding action

Use the canonical production UI:

`https://saas.signalboostapp.com/dashboard/cos-learning`

Click **Embed all eligible retained knowledge** once if the UI still shows the 17-row backlog. The route is owner-authenticated and has governed local-runtime readiness/wake logic; do not tell the owner to type the POST endpoint into the browser URL bar.

After success, verify production DB target:

- total 92
- eligible 78
- eligible embedded 78
- pending 0
- quarantined 14
- all eligible vectors 768 dimensions

If the corpus grows again before verification, use live counts rather than hard-coding 92/78.

---

# Semantic evidence reconciliation already merged

PR #1189 previously reconciled the semantic learned-corpus integration with the evidence safeguards. Preserve these rules:

- `relevance_rejected` rows are excluded from semantic RPC/backfill
- structured objects are JSON-serialized rather than becoming `[object Object]`
- semantic corpus results still pass domain compatibility
- Knowledge Graph excludes `predicate = excluded_from_cos_retrieval`
- Enterprise Memory uses stronger semantic/domain ranking rather than permissive lexical matching
- weak related evidence must not be cited
- zero factual citations is valid
- procedural `[SK#]` evidence is not factual-evidence credit

Do not weaken those safeguards to increase retrieval counts.

---

# Canonical tenant-latency benchmark

Use this exact prompt when validating the COS evidence path:

> A multi-tenant SaaS suddenly shows normal database CPU and memory, but API p95 latency triples only for enterprise tenants. Smaller tenants remain unaffected. No deployment occurred and overall traffic is unchanged. Diagnose the most likely architectural causes, rank them, and explain how you would distinguish between them without making production changes

Expected strong reasoning shape from local COS/Qwen:

1. tenant-size/cardinality threshold causing query-plan/selectivity regime change `[SK1]`
2. enterprise-tier worker/thread/connection-pool/bulkhead queue saturation `[SK1]`
3. enterprise working-set cache threshold/eviction/thrash `[SK1]`
4. enterprise-only dependency/middleware/routing/shard/replica/region degradation `[SK1]`

Each cause should have concrete observables and falsifiers.

## Benchmark cache state

Old benchmark cache entries were deliberately removed so the next benchmark can be fresh.

Latest production verification:

- **benchmark exact-cache remaining: 0**
- **benchmark semantic-answer record remaining: 0**

Do **not** wipe unrelated cache rows.

The user's last pasted latency answer/provenance replayed the old `2026-08-14T04:08:48.225Z` cached answer. That happened **before** the targeted cache purge. The next exact benchmark run should therefore be a new execution.

Because PR #1195 added the Cognitive Council, the fresh benchmark may now activate Council for complex/high-consequence diagnostic work. That is acceptable. Council opinions are advisory review artifacts, not factual evidence and not a confidence booster.

### Fresh benchmark validation sequence

After the newest 17 eligible rows are embedded:

1. Run the exact benchmark **once** in production.
2. Immediately ask for complete provenance.
3. Verify whether local `qwen2.5-coder:32b` ran.
4. Inspect Learned Corpus semantic retrieval after the now-embedded corpus.
5. Inspect exact selected corpus row ids/subjects/source URIs/similarity if false positives remain; do not tune thresholds from counts alone.
6. Verify irrelevant SignalBoost Enterprise Memory is not injected/cited for the DB-latency question.
7. Verify `[SK1]` is cited only as procedural guidance.
8. Verify external AI remains false unless the normal confidence/evidence gate genuinely requires escalation.

---

# Cognitive Council — PR #1195

PR #1195 merged to `main` before the provenance fix:

- Council merge head: `8f49a6a97c5d33dddcefe598f3f73f5579d00858`
- production deployment at that point: `dpl_HPC1UFwWoUAkyT3aGud8eHXpsmgH` READY

The Council is a bounded independent specialist-deliberation layer for metacognitively uncertain / complex / high-consequence work.

Core doctrine to preserve:

- it does not replace the primary COS reasoner
- it does not vote truth into existence
- Council confidence does not become answer confidence
- member opinions are not factual evidence
- deterministic/tool evidence outranks Council opinion
- roles start with neutral credibility until enough externally verified outcomes exist
- routine strong/low-risk work should stay off the Council path

Canonical file: `saas/lib/ai/cos/cognitiveCouncil.ts`.

---

# External-provider provenance defect — fixed by PR #1196

A political/general-knowledge test exposed a truthful-provenance defect.

User asked approximately:

`who is the worse US president of all times?`

Local COS/Qwen produced a neutral historian-style answer, but specificity scoring capped COS confidence at 0.50, below the 0.72 escalation threshold.

Production runtime logs showed the actual external chain:

1. Anthropic attempted and failed because the Anthropic account had insufficient credit.
2. OpenAI attempted and failed with HTTP 429 / insufficient quota.
3. **Gemini succeeded and produced the external fallback answer.**

Before PR #1196, authoritative provenance displayed:

`External AI Provider: unknown — invoked.`

Root cause: the provider router knew the concrete winning provider/model, but the text/backup-continuity path returned only text and dropped provider/model metadata before `cos-primary` assembled authoritative provenance.

## PR #1196 behavior

PR #1196 (`COS: preserve actual external provider provenance`) is merged.

Merge SHA:

- **`137c7ba521f37b0a5019f915366dc24533aa0f1e`**

It now:

- preserves actual provider + model metadata through the governed COS text gateway
- uses request-scoped provider tracing, not unsafe global last-provider state
- distinguishes live provider execution from cached text reuse
- records the actual Gemini/Anthropic/OpenAI winner in answer provenance
- prevents a cache replay from being reported as a fresh external-provider invocation
- keeps raw provider-router access behind the COS blueprint boundary
- includes regression coverage for OpenAI unavailable → Gemini wins → provenance records Gemini/model + fallback used

An intermediate preview failed the blueprint guard with:

`direct_provider_router_import:app/api/cos-primary/route.ts`

That was fixed before merge by consuming the provider trace through the approved COS gateway. Final PR head `a25ffb981d59cac26cbdcca6c7e45e7ebe6a9432` had a READY preview and PR #1196 was merged.

## Runtime verification still needed

Code + production deployment are green, but the **post-merge user-facing provenance behavior has not yet been re-exercised in this conversation**.

Next agent should run a low-confidence/general-knowledge request that actually escalates, then immediately request authoritative provenance. If the same provider account state still applies, expected winner is Gemini; however do not hard-code the Gemini model name from memory—verify the live provenance/telemetry.

Expected result shape:

- Local Reasoning Engine: `independent-local:qwen2.5-coder:32b` — invoked
- External AI Provider: **actual provider / actual model**, not `unknown`
- no false material-use claims for EM/KG/corpus/skills if they were only consulted

If provenance still shows unknown, inspect `cos-escalation-audit`, provider trace, backup continuity payload, and the stored turn provenance before changing code.

---

# Current external-provider operational fact

At the observed 2026-08-14 05:08Z request:

- Anthropic account returned insufficient-credit error
- OpenAI returned 429 `insufficient_quota` / credit exhausted
- Gemini returned a successful response

This is a dated runtime observation, not a permanent provider configuration fact. Re-check before acting on billing/provider assumptions. Do not modify billing or provider policy without explicit owner instruction.

COS doctrine remains local/private first; external providers are last-resort teachers/fallbacks.

---

# Provenance doctrine

Keep this invariant:

`retrieved → relevant → selected → injected → cited`

Material contributors only are listed as material.

For cache replays:

- do not claim a fresh local reasoning call
- do not claim a fresh external provider call
- retain the generating turn's origin separately

For fresh external fallback:

- record actual provider and actual model
- record whether it was a fallback from another requested provider
- do not use `unknown` when the execution boundary has concrete metadata

---

# Remaining high-value next work

Priority order:

1. **Embed the 17 newly eligible corpus rows** and verify live DB/vector counts.
2. **Run the exact latency benchmark fresh** and immediately inspect complete provenance under semantic retrieval + Council.
3. **Runtime-test PR #1196** with a real external escalation and verify provider/model is concrete rather than `unknown`.
4. If semantic false positives remain, inspect the actual selected learned-corpus rows before threshold tuning.
5. Inspect `autoPromoteLearning.ts` and guarantee `benchmark_fixture` can never auto-promote into durable factual KG knowledge. The benchmark fixture is test material, not production truth.
6. Continue Council verification/disagreement-learning work only after the above runtime proofs are clean.

Do not declare COS semantic retrieval finished merely because embeddings exist. Runtime evidence-selection quality is the next proof.

---

# Safe production SQL checks

Embedding counts:

```sql
select
  count(*)::int as total,
  count(*) filter (
    where coalesce(fact_extraction_error,'') not ilike 'relevance_rejected:%'
  )::int as eligible,
  count(*) filter (
    where embedding is not null
      and coalesce(fact_extraction_error,'') not ilike 'relevance_rejected:%'
  )::int as eligible_embedded,
  count(*) filter (
    where embedding is null
      and coalesce(fact_extraction_error,'') not ilike 'relevance_rejected:%'
  )::int as eligible_pending,
  count(*) filter (
    where coalesce(fact_extraction_error,'') ilike 'relevance_rejected:%'
  )::int as quarantined
from cos_continuous_learning;
```

Vector dimensions:

```sql
select vector_dims(embedding) as dims, count(*)
from cos_continuous_learning
where embedding is not null
group by vector_dims(embedding);
```

Expected dimension: 768.

Do not expect raw `embedding is null` to become zero because quarantined rows intentionally remain unembedded.

---

# Project identifiers

- Supabase project: `qpblefwtnbivuusxmabv`
- Vercel project: `prj_QElaxvA1fbFjhVraIzamxRha78Du`
- Vercel team: `team_jgndR6Eo6QyP2Fen9UbHy2Q5`
- SignalBoost org id: `8c70a96d-8d2d-4b25-b413-0e5ffb38131f`
- canonical production app: `saas.signalboostapp.com`

---

# Non-negotiables for takeover

- Reconcile concurrent-agent work; never overwrite a newer merge from memory.
- Exact deployment state wins over assumptions.
- Do not weaken evidence gates to make counts look better.
- Do not embed relevance-rejected rows.
- Do not wipe unrelated caches.
- Do not present Council opinions as factual evidence.
- Do not treat external-model prestige as truth.
- Preserve tenant isolation, human approval boundaries, auditability, and provider portability.
- Only report features/runtime behavior that are actually verified.
